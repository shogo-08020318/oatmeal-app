<div class="flex items-center min-h-screen">
  <div class="container mx-auto">
    <div class="max-w-md mx-auto">
      <div class="text-center">
        <h1 class="text-3xl font-semibold text-gray-700 dark:text-gray-200"><%= t('.title') %></h1>
        <p class="py-3">登録したメールアドレスを入力してください</p>
      </div>
      <div class="m-7">
        <%= form_with url: password_resets_path, local: true do |f| %>
          <div class="mb-4">
            <%= f.label User.human_attribute_name(:email), class: "block mb-2 text-sm text-gray-600 dark:text-gray-400" %>
            <%= f.email_field :email, placeholder: User.human_attribute_name(:email), class: "w-full px-3 py-2 placeholder-gray-300 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-orange-100 focus:border-orange-300 dark:bg-gray-700 dark:text-white dark:placeholder-gray-500 dark:border-gray-600 dark:focus:ring-gray-900 dark:focus:border-gray-500" %>
          </div>
          <div class="mb-6">
            <%= f.submit t('defaults.submit'), class: "w-full p-2 lg:p-2 text-white text-lg lg:text-xl bg-orange-500 rounded-md focus:bg-orange-600 focus:outline-none" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
